<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.droptech.joselluch.meuequip5">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconoinicio"
        android:label="@string/nombreApp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".meuEquip"
            android:label="@string/nombreApp"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".encuestas"
            android:label="@string/title_activity_prueba2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".agradecimientos"
            android:label="@string/title_activity_agradecimientos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".opiniones"></activity>
        <activity android:name=".ajustes"></activity>
    </application>

</manifest>